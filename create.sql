CREATE TABLE people (
    id INTEGER,
    tag TEXT,
    name TEXT,
    age INTEGER,
    balance INTEGER,
    is_admin BOOLEAN
);

INSERT INTO people (id, tag, name, age, balance, is_admin)
VALUES (3, 'sally', 'Sally Wagoner', 18, 100, true);

SELECT * FROM people;
7017017018485908
7017018485908