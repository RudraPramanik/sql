CREATE TABLE transaction (
    id INTEGER,
    recipient_id INTEGER,
    sender_id INTEGER,
    note TEXT,
    amount INTEGER
);

CREATE TABLE transactions (
    id INTEGER, recipient_id INTEGER, sender_id INTEGER, note TEXT, amount INTEGER);
